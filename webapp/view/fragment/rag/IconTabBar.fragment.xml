<c:FragmentDefinition
    xmlns="sap.m"
    xmlns:c="sap.ui.core"
>
    <IconTabBar
        class="sapUiNoContentPadding sapMITH"
        selectedKey="{Utility>/Function}"
        select="onIconTabChange"
        backgroundDesign="Transparent"
    >
        <items>
            <IconTabFilter
                key="CancellaRilievo"
                text="{i18n>iconTabCancellaRilievo}"
                icon="sap-icon://list"
                visible="{Utility>/EnableCancellaRilievo}"
            />
            <IconTabSeparator visible="{Utility>/EnableRegistraRilievo}" />
            <IconTabFilter
                key="RettificaRilievo"
                text="{i18n>iconTabRettificaRilievo}"
                icon="sap-icon://list"
                visible="{Utility>/EnableRettificaRilievo}"
            />
            <IconTabSeparator visible="{Utility>/EnableRegistraRilievo}" />
            <IconTabFilter
                key="RegistraRilievo"
                text="{i18n>iconTabRegistraRilievo}"
                icon="sap-icon://list"
                visible="{Utility>/EnableRegistraRilievo}"
            />
            <IconTabSeparator visible="{Utility>/EnableRegistraRilievo}" />
            <IconTabFilter
                key="RegistraRilievo"
                text="{i18n>iconTabRegistraRilievo}"
                icon="sap-icon://list"
                visible="{Utility>/EnableRegistraRilievo}"
            />
            <IconTabSeparator visible="{Utility>/EnableRegistraRilievo}" />
            <IconTabFilter
                key="Validazione"
                text="{i18n>iconTabValidazione}"
                icon="sap-icon://list"
                visible="{Utility>/EnableValidazione}"
            />
            <IconTabSeparator visible="{Utility>/EnableValidazione}" />
            <IconTabFilter
                key="VerificaConferma"
                text="{i18n>iconTabVerifica}"
                icon="sap-icon://list"
                visible="{Utility>/EnableVerificaConferma}"
            />
            <IconTabSeparator visible="{Utility>/EnableVerificaConferma}" />
            <IconTabFilter
                key="Dettaglio"
                text="{i18n>iconTabDetail}"
                icon="sap-icon://detail-view"
            />
            <IconTabSeparator />
            <IconTabFilter
                key="Workflow"
                text="{i18n>iconTabWorkflow}"
                icon="sap-icon://history"
            />
            <IconTabSeparator />
            <IconTabFilter
                key="FascicoloElettronico"
                text="{i18n>iconTabFascicoloElettronico}"
                icon="sap-icon://documents"
            />
        </items>
    </IconTabBar>
</c:FragmentDefinition>
